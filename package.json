{
  "name": "periodo-client",
  "version": "3.0.0-pre.2",
  "author": "Patrick Golden",
  "description": "Client for browsing and editing PeriodO data.",
  "devDependencies": {
    "babel-minify": "^0.4.3",
    "blue-tape": "^0.2.0",
    "browserify": "^14.3.0",
    "common-shakeify": "^0.2.0",
    "exorcist": "^0.4.0",
    "fake-indexeddb": "^1.0.6",
    "lerna": "^2.5.1",
    "redux-mock-store": "^1.1.0",
    "source-map": "^0.4.4",
    "tape": "^4.8.0",
    "watchify": "^3.3.1"
  },
  "browserify": {
    "transform": [
      "common-shakeify"
    ]
  },
  "scripts": {
    "prepack": "make clean && make",
    "test": "tape $(find modules -path */test/*.js -not -path */node_modules/*)",
    "start": "serve -p 8000"
  }
}
